name: fader_with_buttons
description: Showing the use of the Fader with buttons
version: 1.0.1
publish_to: none

environment:
    sdk: ">=2.12.0-8.3.pre <3.0.0"

dependencies:
    flutter:
        sdk: flutter
    flutter_fader:
        path: ../../

dev_dependencies:
    flutter_test:
        sdk: flutter
flutter:
    uses-material-design: true
